<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ns.sql.FruitMapper">
	<resultMap type="item" id="selectItemRM">
		<result column="item_id" property="itemNumber"/>
		<result column="item_name" property="name"/>
		<result column="price" property="price"/>
		<result column="description" property="description"/>				
		<result column="picture_url" property="url"/>				
		<result column="count" property="count"/>				
	</resultMap>
	
	
	<insert id="addProduct">
	
		
	</insert>
	<select id="findProductByName" parameterType="string">
		SELECT 
		id, name, maker, price
		FROM myproduct WHERE name=#{VALUE}
	</select>
	<select id="findProductByMaker" parameterType="string">
		SELECT 
		id, name, maker, price
		FROM myproduct WHERE maker=#{VALUE}
	</select>
	<select id="findProducts">
		SELECT 
		id, name, maker, price
		FROM myproduct
	</select>


</mapper>

